# 開発への参加

現在は公開準備中です。公開後はこのリポジトリでIssueと変更提案を受け付けます。開発・公式配信の正本はメンテナーのPrivateリポジトリです。

## 開発

HTML/CSS/JavaScriptの静的アプリです。npmはブラウザテスト用で、実行環境にNode.jsを常駐させません。起動・ビルドはREADMEを参照してください。

ブラウザテストは `BASE_URL` に指定したローカルHTTP環境へ接続します。自分の検証サーバーだけを指定してください。テストは架空の設定値を使用し、実際の授業データを必要としません。

最低限の確認: 初期表示、プリセット、入力境界、開始/一時停止/再開/リセット、0到達、Space/Esc、授業モード/全画面、保存不可・不正保存値、音声開始、タブ復帰、PC/横向きタブレット、ソース取得。印刷成果物はなく印刷試験は対象外です。

改変時はコードと対応ソースZIPを同じビルドで生成し、`config.js`の版表示を更新します。Font Awesomeなどの原通知を削除しないでください。

## Issue / PR

- 恒久ブランチは`main`だけです。`dev`は置きません。提案者はforkや一時ブランチで作業し、`main`向けにPRを提出します。
- 不具合は再現手順、期待結果、ブラウザを記載します。
- 学校名、名簿、成績、児童生徒や教員が特定できる画面を貼らず、架空データで再現します。
- PRには変更理由と確認結果を添えます。`main`を対象に変更提案を提出します。採用分は作者・ライセンス・通知を維持してメンテナーがPrivate正本へ取り込み、検証後に再生成して反映します。提案をそのままマージせず、再生成コミットと対応づけて完了する場合があります。
- 自分が提供する権利を持つ変更だけを提出し、対象ファイルの既存ライセンスで配布できることを確認してください。第三者コードを追加する場合は出典・権利条件を添えます。
- 脆弱性は公開Issueに書かず、[SECURITY.md](SECURITY.md)の手順に従います。

## 共通部品

この候補には共通部品の固定コピーを含めています。実行時に別repoや公式サイトから最新版を読み込みません。同期対象の変更は、元の共通正本と配布先を確認してから行います。
